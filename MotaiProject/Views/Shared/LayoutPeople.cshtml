@using MotaiProject.Models
@{
    tEmployee emp = Session[CSession關鍵字.SK_LOGINED_EMPLOYEE] as tEmployee;
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    <script src="../../Scripts/jquery-3.5.1.min.js"></script>
    <link href="../../Scripts/bootstrap/bootstrap.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet">

    <style>
        #wrapper {
            display: flex;
        }

        body {
            background: url("/images/Employee_login.png");
            background-position: center 0;
            background-repeat: no-repeat;
            background-attachment: fixed;
            background-size: cover;
        }

        #wrapper #content-wrapper {
            background-color: #f8f9fc;
            width: 100%;
            overflow-x: hidden;
        }

            #wrapper #content-wrapper #content {
                flex: 1 0 auto;
            }

        .sidenav {
            height: 100%; /* Full-height: remove this if you want "auto" height */
            width: 160px; /* Set the width of the sidebar */
            position: fixed; /* Fixed Sidebar (stay in place on scroll) */
            z-index: 1; /* Stay on top */
            top: 0; /* Stay at the top */
            left: 0;
            background-color: #111; /* Black */
            overflow-x: hidden; /* Disable horizontal scroll */
            padding-top: 20px;
        }

            /* The navigation menu links */
            .sidenav a {
                padding: 6px 8px 6px 16px;
                text-decoration: none;
                font-size: 25px;
                color: #000000;
                display: block;
            }

                /* When you mouse over the navigation links, change their color */
                .sidenav a:hover {
                    color: #f1f1f1;
                }

        /* Style page content */
        .main {
            margin-left: 160px; /* Same as the width of the sidebar */
            padding: 0px 10px;
        }

        /* On smaller screens, where height is less than 450px, change the style of the sidebar (less padding and a smaller font size) */
        @@media screen and (max-height: 450px) {
            .sidenav {
                padding-top: 15px;
            }

                .sidenav a {
                    font-size: 18px;
                }
        }
    </style>

</head>
<body id="page-top">

    <!-- Page Wrapper -->
    @*<div id="wrapper">

        <!-- Sidebar -->
        <ul class="navbar-nav sidebar accordion" id="accordionSidebar">

            <!-- Sidebar - Brand -->

            <li class="sidebar-brand-text mx-3">
                <span>@Html.ActionLink("人事首頁", "People首頁", "People", new { area = "" }, new { @class = "navbar-brand" })</span>
            </li>


            <!-- Divider -->
            <hr class="sidebar-divider my-0">

            <li class="nav-item">
                <i class="fas fa-fw fa-cog"></i>
                <span>@Html.ActionLink("消息頁面", "員工看消息", "People", new { area = "" }, new { @class = "navbar-brand" })</span>

            </li>
            <!-- Nav Item - Dashboard -->
            <li class="nav-item active">
                <i class="fas fa-fw fa-tachometer-alt"></i>
                <span>@Html.ActionLink("員工檢視", "人員檢視", "People", new { area = "" }, new { @class = "navbar-brand" })</span>

            </li>
            <li class="nav-item active">
                <i class="fas fa-fw fa-tachometer-alt"></i>
                <span>@Html.ActionLink("員工新增", "新增員工", "People", new { area = "" }, new { @class = "navbar-brand" })</span>

            </li>


            <li class="nav-item">
                <a class="nav-link" href="charts.html">
                    <i class="fas fa-fw fa-chart-area"></i>
                    <span>@Html.ActionLink("登出", "員工登出", "Employee")</span>
                </a>
            </li>

        </ul>

        <div class="container body-content;">
            <div style="background-color:rgba(255, 255, 255,0.9) ;width:70vw; border-radius:10px; margin-top:50px; margin-left:-30px;" class="main">
                @RenderBody()
            </div>
            <hr />
            <footer>
                <p>&copy; @DateTime.Now.Year - 墨臺</p>
            </footer>
        </div>
    </div>*@


    <div id="wrapper">



        <!-- Side navigation -->
        <div class="sidenav" style="background-color:#A7674C">
            <a class="sidebar-brand d-flex align-items-center justify-content-center" href="@Url.Action("首頁","Customer")">
                <div class="sidebar-brand-icon rotate-n-15">
                    <i class="fas fa-laugh-wink">
                        <img style="height: auto;width: 100px;padding-bottom:40px" href="#" src="../../Content/墨臺Logo.png" alt="">
                    </i>
                </div>
            </a>
            @Html.ActionLink("人事首頁", "People首頁", "People", new { area = "" }, new { @class = "navbar-brand" })
            @Html.ActionLink("消息頁面", "員工看消息", "People", new { area = "" }, new { @class = "navbar-brand" })
            @Html.ActionLink("員工檢視", "人員檢視", "People", new { area = "" }, new { @class = "navbar-brand" })
            @Html.ActionLink("員工新增", "新增員工", "People", new { area = "" }, new { @class = "navbar-brand" })
            @Html.ActionLink("登出", "員工登出", "Employee", new { area = "" }, new { @class = "navbar-brand" })
        </div>



        <div class="container body-content main" style="padding-left:160px;min-width:780px">
            <div class="row" style="height:30px"></div>
            <div class="container-fluid bg-white" style="">
                @RenderBody()
            </div>

            <hr />
            <footer>
                <p>&copy; @DateTime.Now.Year - 墨臺</p>
            </footer>
        </div>
    </div>

    <script src="../../Scripts/jquery-3.5.1.min.js"></script>
    <script src="../../Scripts/jquery.validate.min.js"></script>
    <script src="../../Scripts/jquery.validate.unobtrusive.min.js"></script>
    <script src="../../Scripts/bootstrap.bundle.min.js"></script>
    <link href="../../Scripts/bootstrap/bootstrap.min.css" rel="stylesheet" />

</body>
</html>